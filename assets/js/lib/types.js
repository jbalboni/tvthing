// @flow

export type User = {
  id: number,
  email: string
};

export type Watchlist = {
  id: number,
  name: string
};

export type Show = {
  id: number,
  name: string
};

export type Result = {
  title: string,
  id: number,
  artwork_208x117: string
};
